[tool.poetry]
package-mode = false

[project]
name = "matrix-analysis"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.1.0",
    "numpy>=1.25.0",
    "matplotlib>=3.7.2",
]
